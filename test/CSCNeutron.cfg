process CSCDigitizerTest = {
   source = PoolSource {
     untracked vstring fileNames = {'file:muon100.root'}
      untracked int32 maxEvents = -1
     untracked uint32 debugVebosity = 10
     untracked bool   debugFlag     = true

   }

  service = MessageLogger {
     untracked vstring debugModules = {}
  }

   include "Geometry/MuonCommonData/data/muonIdealGeometryXML.cfi"
   include "Geometry/CSCGeometry/data/cscGeometry.cfi"
   include "MagneticField/Engine/data/volumeBasedMagneticField.cfi"



   include "SimMuon/CSCDigitizer/data/cscNeutronWriter.cfi"
   # write real muons, just to see
   replace cscNeutronWriter.neutronTimeCut = 0
# provide a scheduler path
   path p1 = { cscNeutronWriter }
}

